{
  "name": "@tomcp/cli",
  "version": "0.1.3",
  "description": "CLI for Transfer over MCP — test transfers from the terminal",
  "type": "module",
  "bin": {
    "tomcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tomcp/types": "^0.1.0",
    "@tomcp/server": "^0.1.0",
    "@tomcp/client": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bhanquier/tomcp.git",
    "directory": "packages/cli"
  },
  "keywords": [
    "mcp",
    "tomcp",
    "transfer",
    "cli"
  ],
  "publishConfig": {
    "access": "public"
  }
}
